import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maryamsherif.sunnahtracker',
  appName: 'SunnahTracker',
  webDir: 'dist'
};

export default config;
